---
"hadronomy-skills": minor
---

Add `typst-author` (in-progress): author idiomatic, compilable Typst documents behind a compile-verify loop. Replaces the unmanaged `typst-writer` copy with a router SKILL.md, branch-only references (syntax traps, packages, bibliography), a toolchain preflight gate, and a `typstyle` format gate.

```bash
npx skills@latest add hadronomy/skills --skill=typst-author
```
