---
"@hadronomy/opencode-handoff-plugin": patch
---

Update the live-server notes: `transfer` round-trips against beta-19135, the fork boundary still matches the beta-19135 tarball, and render stays on `create` plus `synthetic` since the plugin context still exposes no fork, export, or import.
