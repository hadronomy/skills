---
"hadronomy-skills": minor
---

Add the OpenCode session-handoff plugin under `plugins/handoff/`: one `transfer` RPC from structured intent to resumable pointer, with capture, redact, and render stages behind it.
