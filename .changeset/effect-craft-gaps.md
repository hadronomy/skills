---
"hadronomy-skills": patch
---

Fold the session-handoff field report into `effect-craft`: namespace binding by name, `binding.verb` calls, domain-verb method names, `Match.discriminator` on data unions, `Layer.provide` discard semantics, defect-to-typed conversion at `never` boundaries, a pinned v4 RC delta note, colocated protocol modules, checked-schema validation, `satisfies` wire pins, and doctest peer-range spikes.
